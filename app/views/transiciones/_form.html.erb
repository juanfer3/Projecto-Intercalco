<%= simple_form_for(@transicion) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :desarrollo_de_tinta %>
    <%= f.association :tinta_formulada %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
