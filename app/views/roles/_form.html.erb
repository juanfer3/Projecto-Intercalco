<%= form_with(model: rol, local: true) do |form| %>
  <% if rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rol.errors.count, "error") %> prohibited this rol from being saved:</h2>

      <ul>
      <% rol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cargo %>
    <%= form.text_field :cargo, id: :rol_cargo %> <br>
    <label class="form-check-label">
      Este administrará maquina de impresión?
      <%= form.check_box :administrador_maquina,{}, class:'form-check-input' %>
      <input type="checkbox" class="form-check-input">
    </label>
    <label for="[object Object]"></label>

  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
