<%= simple_form_for(@tinta_formulada,remote:true) do |f| %>
  <%= f.error_notification %>

  <div class="card">

    <div class="my_card_contactos_form card-header">
          <div class="">
            <h3 class="">Tintas Formuladas</h3>
          </div>

          <div class="">
            <button type="button"  class="btn btn-outline-danger" id="CerrarVentana"><span><i class="ion-reply-all"></i></span></button>
          </div>
    </div>

    <div class="container">
      <br>
        <div class="card-block">
          <div class="my_card_pedidos_form">

            <div class="col-4">
              <%= f.input :codigo %>
            </div>
            <div class="col-4">
              <%= f.input :descripcion %>
            </div>
            <div class="col-4">
              <%= f.input :pantone %>
            </div>
            <div class="col-4">
              <label for="">Linea De Color</label> <br>
              <%= f.select :linea_de_color_id,LineaDeColor.all.map {|i|[i.nombre, i.id]}, {},
              {required: true, autofocus: 'true', class: "js-example-tags form-control",id: 'select_linea'}
               %>
            </div>

            <div class="col-4">
              <label for="">Selección De Malla</label>
              <%=
              f.select :malla_id,
              Malla.all.map{|i|[i.nombre, i.id]},{},
              {required: true, autofocus: 'true', class: "js-example-tags form-control", id: "select_malla"}
               %>
            </div>


            <div class="col-12">
              <hr>
            </div>


            <div class="col-12">
              <div class="my_card_contactos_form">
                <div >
                  <h3>Formula</h3>
                </div>
                <div >
                  <%= link_to_add_formula('<button type="button" id="pruebaSelect" class="btn btn-outline-primary"><span>Agregar <i class="ion-beaker"></i></span></button>'.html_safe, f, :formulas_tinta, class: '') %>
                </div>
            </div>
            <div class="">

              <table class="table">
                <thead>

                </thead>
                <tbody class='fields_formula'>
                  <%=f.fields_for :formulas_tinta do |builder|%>

                     <%= render 'formula_tinta', :f => builder%>

                  <%end%>
                </tbody>
                </table>

            </div>

            <div class="">

              <table class="table">
                <thead>

                </thead>
                <tbody class=''>
                  <%=f.fields_for :transiciones do |builder|%>

                     <%= render 'transicion', :f => builder%>

                  <%end%>
                </tbody>
                </table>

            </div>
            </div>
          </div>
        </div>
        <div class=" col-3 offset-md-9">

          <%= button_tag type: 'submit', class: "btn btn-outline-success" do %>
            <span>Crear Tinta Nueva
              <i id="iconoSubmit"class="ion-paper-airplane" aria-hidden="true"></i>
            </span>
           <% end %>

        </div>
      <br>

    </div>
  </div>

  <br>




    <div >



    <br>


<% end %>
