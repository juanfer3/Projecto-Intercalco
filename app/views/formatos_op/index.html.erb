<p id="notice"><%= notice %></p>

<h1>Formatos Op</h1>

<table class="table">
  <thead>
    <tr>
      <th>Referencia de orden</th>
      <th>User</th>
      <th>Maquina</th>
      <th>Montaje</th>
      <th>Pieza a decorar</th>

      <th>Linea de color</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @formatos_op.each do |formato_op| %>
      <tr>
        <td><%= formato_op.referencia_de_orden %></td>
        <td><%= formato_op.montaje.cliente.nombre %></td>
        <td><%= formato_op.user.nombre %></td>
        <td><%= formato_op.maquina.nombre %></td>
        <td><%= formato_op.pieza_a_decorar.nombre %></td>

        <td><%= formato_op.linea_de_color.nombre %></td>
        <td><%= formato_op.material %></td>

        <td><%= link_to 'Show', formato_op %></td>
        <td><%= link_to 'Edit', edit_formato_op_path(formato_op) %></td>
        <td><%= link_to 'Destroy', formato_op, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Formato Op', new_formato_op_path %>

<button class="btn btn-success" data-toggle="modal" data-target="#modalExcel">Importar Clientes Desde Excel</button>
<br>
<br>

<div class="modal fade fullscreen-modal " id="modalExcel" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content col-12">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle">Importar Clientes Desde Excel</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <div class="container">
          <h1>Instrucciones:</h1>
          <h6> <strong>-</strong> Para importar Clientes asegurese de que el orden las las columnas sean los siguientes: <br> &nbsp; <strong> Nombre del Cliente, Nit, Dirección, Telefono.</strong>  </h6>
          <h6><strong>-</strong> Además El archivo tendrá que pertenecer a alguna de estas extenciones:<br> &nbsp; <strong>".xls", ".xlsx", ".csv". </strong></h6>
            <%= form_tag import_fop_from_excel_path, multipart: true do %> <br>
            <%= file_field_tag :file %> <br> <br>
            <%= submit_tag "Importar", :class=> "btn btn-primary"%>
          <% end %>

        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>

      </div>

    </div>
  </div>
</div>
