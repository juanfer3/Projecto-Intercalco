<!--ORDENES DE IMPRESION





<% if @orden.impresion == true %>

  <%= link_to '<button class="btn btn-outline-success"><span><i class="ion-checkmark-round"></i></span></button>'.html_safe, confirmar_impresion_path(@orden_produccion), remote: true %>
<% else %>
  <%= link_to '<button class="btn btn-outline-warning"><span><i class="ion-qr-scanner"></i></span></button>'.html_safe, confirmar_impresion_path(@orden_produccion), remote: true %>
<% end %>
-->
