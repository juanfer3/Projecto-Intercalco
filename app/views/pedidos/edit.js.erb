$("#myModal").html("<%= j render "new"%>");
$('#myModal').modal('show');

$('<%= j render "form" %>').on('hidden.bs.modal', function (e) {
  $(this).remove();
  $('.modal-backdrop').remove();
}).modal();